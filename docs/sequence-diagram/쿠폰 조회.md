```mermaid
sequenceDiagram
participant Client
participant UserCouponController
participant UserCouponService
participant UserCouponRepository

    Client->> UserCouponController: 보유 쿠폰 조회 (userId)
    UserCouponController ->> UserCouponService: 조회 요청 전달
    UserCouponService ->> UserCouponRepository: 쿠폰 조회
alt 보유한 쿠폰이 없을 때
UserCouponRepository -->> UserCouponService: 보유쿠폰 X 반환
UserCouponService -->> UserCouponController: 보유쿠폰 X 예외 발생
UserCouponController --> Client: 404 Not Found
else 보유한 쿠폰이 있을 때
UserCouponRepository -->> UserCouponService: 보유 쿠폰 목록 반환
UserCouponService -->> UserCouponController: 보유 쿠폰 목록 응답
UserCouponController -->> Client: 쿠폰 목록 응답
end